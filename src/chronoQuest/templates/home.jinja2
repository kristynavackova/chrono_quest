<!doctype html>
<html lang="cs">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>{{ title }}</title>
    <style>
      body { font-family: system-ui, -apple-system, Segoe UI, Roboto, Arial, sans-serif; margin: 2rem; }
      .wrap { max-width: 780px; margin: 0 auto; }
      h1 { font-size: 2rem; margin-bottom: .5rem; }
      .muted { color: #666; }
      ul { padding-left: 1.25rem; }
      li { margin: .25rem 0; }
      .card { border: 1px solid #e5e5e5; border-radius: 12px; padding: 1rem 1.25rem; box-shadow: 0 1px 3px rgba(0,0,0,.06); }
      code { background: #f6f8fa; padding: .15rem .35rem; border-radius: 6px; }
      a.btn { display:inline-block; padding:.5rem .8rem; border:1px solid #ccc; border-radius:8px; text-decoration:none; }
    </style>
  </head>
  <body>
    <div class="wrap">
      <h1>{{ title }}</h1>
      <p class="muted">Zkušební landing pro vzdělávací aplikaci ChronoQuest (Pyramid + Jinja2).</p>

      <div class="card">
        <h2>Roční témata</h2>
        <ul>
          {% for t in themes %}
            <li><strong>{{ t.year }}</strong> — {{ t.name }}</li>
          {% endfor %}
        </ul>
      </div>

      <p>
        <a class="btn" href="/themes">JSON: /themes</a>
      </p>
    </div>
  </body>
</html>
